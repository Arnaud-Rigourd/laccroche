<!DOCTYPE html>
<html>
  <head>
    <title>Laccroche - create your musical network</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta title="L'accroche | Make you create and improve your musical network">
    <meta descrition="'accroche let you connect within the huge comunauty of musical creators">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= favicon_link_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <link rel="stylesheet" href="https://use.typekit.net/oxr3hup.css">
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>

    <%= render "shared/navbar" %>


  <%= render "shared/flashes" %>
  <%= yield %>


  <% if params[:action] != "top" && params[:action] != "home" %>
    <%= render "shared/footer" %>
  <% end %>

  </body>
</html>
